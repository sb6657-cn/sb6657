# 版本策略

本文档记录 `sb6657.cn` 第三版网站的版本号规则、历史版本溯源和后续升级口径。自 `V3.13.11.20260721` 起启用新规则；此前 `YY.MM.DD` 格式的更新日志作为旧版本记录保留，不做批量改写。

## 版本号格式

面向用户和更新日志的完整版本号使用：

```text
Vmajor.minor.patch.yyyymmdd
```

- `major`：网站大版本。只有整体产品代际或不兼容的重大迭代才增加，由项目负责人决定。
- `minor`：大块功能版本。新增独立业务模块、核心体系，或让已有模块发生能力级跃迁时增加。
- `patch`：修订版本。Bug 修复、UI/交互优化、重构、工程调整和未达到 minor 门槛的功能增强都增加 patch。
- `yyyymmdd`：实际发布当天的本地日期，固定为 8 位，例如 `20260721`。

项目不使用 `const` 构建号和 `_class` 阶段后缀。`major`、`minor`、`patch` 是互相独立的整数，不是小数位。

## 代码中的存储形式

完整展示版本与包元数据表达的是同一个版本，但受 SemVer 格式约束，序列化方式不同：

| 位置 | 格式 | 当前示例 |
| --- | --- | --- |
| 页面右下角、`sbVersion`、源码更新日志标题 | `Vmajor.minor.patch.yyyymmdd` | `V3.15.0.20260816` |
| `package.json` | `major.minor.patch+yyyymmdd` | `3.15.0+20260816` |

`+yyyymmdd` 是标准 SemVer 的构建元数据写法。不要把四段点分版本或前缀 `V` 直接写进 `package.json.version`。

项目使用 `pnpm-lock.yaml` 锁定依赖解析结果。该文件不记录根包发布版本，因此不属于版本号同步点；仅在依赖声明或解析结果变化时由 pnpm 更新，不要手工写入发布版本。

C 端展示时，右下角直接显示完整版本，不再额外添加“版本：”前缀。更新日志时间线以发布日期为节点；同一天的多个版本合并在一个日期节点内，并按新到旧分别展示内容。节点内的标题对旧版本保留 `版本【YY.MM.DD】`，对新版本显示不重复日期的 `版本【Vmajor.minor.patch】`。

## 升级规则

每个准备发布的代码改动批次按以下优先级判断：

1. 产品代际或不兼容重大迭代：`major + 1`，`minor` 和 `patch` 归零。
2. 新增大块功能：`minor + 1`，`patch` 归零。
3. 其他代码改动：`patch + 1`。
4. 日期始终换成实际发布当天的 `yyyymmdd`。

一次任务或一次发布批次包含多项改动时只升级一次，按其中最高级别决定。一天内发布多个独立批次时，每个批次继续递增 patch，日期可以相同。纯内部文档改动默认不升级版本；用户可见发布文档、版本规则调整或用户明确要求时除外。

`minor` 增加时 `patch` 必须归零，例如 `V3.13.13.20260721` 之后的大块功能版本是 `V3.14.0.yyyymmdd`，不是 `V3.14.1.yyyymmdd`。日期必须是一个连续的 8 位字段，不能写成 `V3.14.0.26.07.27`。跨多日开发但只发布一次时，只记录一个版本并使用实际发布日期；确实按天形成多个独立发布批次时逐次增加 patch。是否属于独立发布以更新日志记录的业务批次为准，不以 Git 提交数量判断。

### minor 判定口径

通常计入 minor：

- 新增具有独立入口、独立数据或完整交互闭环的业务模块。
- 新增跨页面的核心平台能力，例如账户体系、实时连接体系。
- 重做核心信息架构或数据模型，并显著改变主要使用方式。
- 已有模块新增足以改变能力层级的大型子系统，例如 AI 从单轮生成升级为带记忆的会话体系。

通常只计入 patch：

- Bug 修复、样式调整、响应式适配、性能优化和代码重构。
- 排序、筛选、导出、入口、提示、归档、展示字段等既有模块增强。
- 工程化、依赖、CI、文档和开发流程调整。
- 季节性视觉效果、彩蛋、外部链接和未形成完整业务模块的小功能。

边界不清时先结合 `docs/更新日志.md` 和 Git 历史判断，再由用户或项目负责人决定；不要仅根据 commit message 中是否出现 `feat` 自动增加 minor。

## 第三版网站 minor 溯源

Git 仓库从 2024 年 9 月 25 日开始记录第三版网站。当时已经具备首页、烂梗列表与分类、投稿、搜索/热榜、相册和星空背景，因此将该状态作为 `3.0.0` 基线。

| 推定版本 | 时间 | 大块功能 | 关键提交 |
| --- | --- | --- | --- |
| `3.0.0` | 2024-09-25 | 第三版网站基础形态 | `934c46d` |
| `3.1.0` | 2024-10-18 | 在线聊天室 | `c3cd7d5` |
| `3.2.0` | 2024-12-11 | 年度 TOP20 多阶段评选 | `54ddd5a` |
| `3.3.0` | 2025-02-03 | tag 式烂梗体系 | `6bb24a7` |
| `3.4.0` | 2025-03-24 | 注册、登录、资料与密码管理组成的用户体系 | `8f35a7a` |
| `3.5.0` | 2025-04-21 | 鱼吧社区及帖子消息体系 | `502b120` |
| `3.6.0` | 2025-05-03 | 斗鱼直播间 WebSocket、开播提醒和贵宾数 | `269bd29` |
| `3.7.0` | 2025-06-02 | Major 赛事竞猜体系 | `d309ea8` |
| `3.8.0` | 2025-06-11 | 赛事烂梗库 | `705f947` |
| `3.9.0` | 2025-06-16 | AI 造梗 | `f0a3b64` |
| `3.10.0` | 2025-09-19/20 | 超级逮虾户战报；日志记为 9 月 19 日，代码于 9 月 20 日提交 | `d757137` |
| `3.11.0` | 2025-09-20 | 布雷德十五勇士榜单 | `43743c7` |
| `3.12.0` | 2025-11-02 | 屏蔽词投稿、投票、匹配和个人查询体系 | `118c4ec` |
| `3.13.0` | 2026-05-31 | AI 记忆、会话和历史记录体系 | `5e89b9d` |
| `3.14.0` | 2026-07-21 | 用户成长与勋章、烂度排行、烂梗擂台、AI 玩梗接龙、签到梗币、生命周期与 DNA 图谱、实时热度墙 | `c870fca` |
| `3.15.0` | 2026-08-16 | 项目迁移至 `sb6657-cn` 组织，部署链路改用 GitHub App，全站仓库入口与文档同步 | （本次迁移批次） |

没有计入 minor 的代表性改动包括搜索词云、点赞、高级搜索、我的投稿、更新日志页面、TOP20/榜单留档、图片下载、月薪喵屏保和时光机入口。这些要么属于已有模块增强，要么是辅助页面、彩蛋或外部入口。

## `3.13` patch 溯源

以 2026 年 5 月 31 日的 AI 会话体系作为 `3.13.0`，按旧更新日志中可识别的发布批次继续推导 patch：

| 推定版本 | 旧日志版本 | 内容概括 |
| --- | --- | --- |
| `3.13.0` | `26.05.31` | AI 记忆、会话和历史记录 |
| `3.13.1` | `26.06.09` | 月薪喵屏保 |
| `3.13.2` | `26.06.11` | History 路由 |
| `3.13.3` | `26.06.25` | 顶部投稿入口 |
| `3.13.4` | `26.07.01` | Agent 流程、Lint 和注册页修复 |
| `3.13.5` | `26.07.04` | 热梗弹窗优化 |
| `3.13.6` | `26.07.10` | 移动端导航重做 |
| `3.13.7` | `26.07.11` | 导航与弹窗层级修订 |
| `3.13.8` | `26.07.12` | 双端 Header 与全局弹窗体系重构 |
| `3.13.9` | `26.07.14` | 列表、投稿和 Footer 调整 |
| `3.13.10` | `26.07.15` | 时光机及首页调整 |
| `3.13.11` | `V3.13.11.20260721` | 启用新版本号机制 |
| `3.13.12` | `V3.13.12.20260721` | 搜索词云跳转和随机烂梗复制热区调整 |
| `3.13.13` | `V3.13.13.20260721` | 通过 URL 标签参数筛选烂梗 |

patch 按更新日志发布批次而不是原始 Git 提交数推导。历史 CI 会自动部署每个源码提交，但旧版本号并未随每次部署更新，直接按提交数计数会失去产品意义。

## `3.14` patch 溯源

以 2026 年 7 月 21 日的大块功能发布作为 `3.14.0`，之后按更新日志中逐日记录的独立发布批次递增 patch。相关前端代码虽然最终集中在 `c870fca` 一次提交中，版本历史仍以原更新日志记录为准：

| 版本 | 发布时间 | 内容概括 |
| --- | --- | --- |
| `3.14.0` | 2026-07-21 | 用户成长、烂度排行、烂梗擂台、签到梗币、生命周期与 DNA 图谱、实时热度墙 |
| `3.14.1` | 2026-07-22 | 新功能页面视觉重做及接口数据匹配修复 |
| `3.14.2` | 2026-07-23 | 移除玩梗接龙、调整用户入口、生命周期分页与响应式布局 |
| `3.14.3` | 2026-07-24 | 烂度排行算法与无限滚动、后端滑动窗口限流 |
| `3.14.4` | 2026-07-25 | 新功能页面统一样式、热度墙模式、勋章梗币与查询优化 |
| `3.14.5` | 2026-07-26 | DNA 关联图谱算法升级 |
| `3.14.6` | 2026-07-30 | 昵称校验与改名限制、DNA 图谱展示和算法优化 |
| `3.14.7` | 2026-07-31 | 新增“弗一把”友情链接与首页推广卡片 |
| `3.14.8` | 2026-07-31 | 修复移动端内容区遮挡星空背景 |
| `3.14.9` | 2026-07-31 | 全站深色模式适配与首页简介内容补充 |
| `3.14.10` | 2026-08-03 | DNA 关联图谱算法升级到 v6 |
| `3.14.11` | 2026-08-06 | 新增合成大猪头小游戏（合成流心西瓜类，全局入口） |
| `3.14.12` | 2026-08-07 | 时光相册自适应网格与组件拆分、`useIsMobile` 迁移至 composables、组件行数与目录规范 |
| `3.14.13` | 2026-08-07 | 合成大猪头离线兜底与重开流关闭修复 |
| `3.14.14` | 2026-08-07 | 修复合成大猪头落球队列不推进与重复落球 |
| `3.14.15` | 2026-08-11 | 全仓迁移 pnpm、部署流水线质量门禁、接口类型契约补齐 |

## 修改代码时的同步清单

后续 AI 或开发者修改代码时必须：

1. 读取当前本地日期和本文件中的当前版本。
2. 按上述口径决定 major、minor 或 patch。
3. 在 `docs/更新日志.md` 新增 `## 版本【Vmajor.minor.patch.yyyymmdd】`，并写简短改动项。
4. 将 `package.json` 根包版本同步为 `major.minor.patch+yyyymmdd`。
5. 将 `src/apis/httpInstance.ts` 的 `sbVersion` 同步为完整展示版本 `Vmajor.minor.patch.yyyymmdd`。
6. 运行 `pnpm run lint`、`pnpm run typecheck` 和 `pnpm run build`，并检查三处版本表达的 major、minor、patch 和日期完全一致。
